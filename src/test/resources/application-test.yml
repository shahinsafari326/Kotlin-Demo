spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/kotlin_demo_test
    username: postgres
    password: admin

  jpa:
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true